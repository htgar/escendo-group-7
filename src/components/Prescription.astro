---
const { prescription } = Astro.props;
import drugs from "../data/drugs.json";
import providers from "../data/providers.json";
---

<div class="card bg-base-100 w-96 shadow-xl m-auto my-3">
    <div class="card-body">
        <h2 class="card-title">Prescription {prescription["id"]}</h2>
        <p>🩺 Provider: {providers[prescription["provider"]]}</p>
        {
            prescription["drugs"].map((drug) => (
                <p>
                    {" "}
                    💊
                    {drug["qty"]} {drugs[drug["id"]]} taken {drug["dose"]}
                </p>
            ))
        }
        <div class="card-actions justify-end">
            <a
                class="btn btn-primary"
                href={"/prescriptions/" + prescription["id"]}>Collect</a
            >
        </div>
    </div>
</div>
